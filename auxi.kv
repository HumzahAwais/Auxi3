#:kivy 2.0.0
#:import NoTransition kivy.uix.screenmanager.NoTransition

<MainPage>:
    name: 'main'
    orientation: 'vertical'
    # Defines app manager and sets the transition to be nothing
    # It also defines which screens are in the screen manager
    AppManager:
        id: am
        transition: NoTransition()
        LoginPage:
        HomePage:
        SignupPage:
        ChatPage:
        TrackPage:
        ProfilePage:

<LoginPage>:

<SignupPage>:

<HomePage>:

<ChatPage>:

<SettingsPage>:
    
<TrackPage>:

<ProfilePage>:
    

